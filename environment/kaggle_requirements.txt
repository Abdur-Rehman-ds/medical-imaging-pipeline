# Run this in a Kaggle notebook cell: !pip install -q -r kaggle_requirements.txt
#
# Deliberately does NOT include torch/torchvision/CUDA — Kaggle's GPU
# notebook image ships a working pinned torch+CUDA build already; pip
# installing over it is the #1 cause of broken-CUDA sessions and wastes
# GPU-hour quota re-downloading multi-GB wheels (Section 2.4 / Risk table
# Section 13). If you need a specific torch version, check it first with:
#   import torch; print(torch.__version__, torch.version.cuda)

monai==1.3.2
nibabel==5.2.1
SimpleITK==2.3.1
hydra-core==1.3.2
omegaconf==2.3.0
mlflow==2.14.1          # or wandb — pick one per FR-3.4; not both
