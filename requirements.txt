# Direct dependencies — local dev: API, tests, lint (Section 2.5 reproducibility).
# Pruned 09-Sep-2026 to packages actually imported by src/ + tests/ (see
# Appendix E). Superseded/deferred deps removed: mlflow (decision 1 -> W&B),
# boto3/psycopg2 (decision 7, local storage), dvc/onnxruntime/SimpleITK/
# hydra-core/prometheus-client (unused). Restore any of these only when code
# imports them.
#
# Machine-generated full pin lives in requirements.lock.txt — Docker installs
# from THAT file, not this one. Regenerate after any change here:
#   python3 -m venv /tmp/lockenv && /tmp/lockenv/bin/pip install -r requirements.txt && { echo header + extra-index-url; /tmp/lockenv/bin/pip freeze; } > requirements.lock.txt
#
# Kaggle training notebook has its own requirements (torch/MONAI preinstalled
# there — Section 2.4); do not install this file on Kaggle.

# torch 2.13.0+cpu comes from the PyTorch CPU wheel index, not plain PyPI:
--extra-index-url https://download.pytorch.org/whl/cpu

# Data / model layer (Section 6.3)
nibabel==5.4.2
numpy==2.5.2
monai==1.6.0
scipy==1.18.1  # not imported by our code; MONAI loads it lazily for FR-4.3 post-processing transforms (connected components, hole-filling)
torch==2.13.0+cpu

# Serving layer (FR-5.x)
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32  # not imported by our code; FastAPI loads it dynamically for file-upload Form/File endpoints (FR-5.1)

# Config
omegaconf==2.3.1

# Testing & lint (Sections 10.3, 11)
httpx==0.28.1  # not imported by our code; fastapi.testclient (used by API tests) requires it
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.5
mypy==2.3.1
