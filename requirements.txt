# Local development requirements — API, frontend tooling, tests, lint.
# For Kaggle training-notebook requirements see environment/kaggle_requirements.txt
# (Kaggle images come with torch/CUDA/MONAI preinstalled; do not reinstall
# torch there — see Section 2.4, Operating Environment).

# Data layer — nibabel, SimpleITK (Section 6.3)
nibabel==5.2.1
SimpleITK==2.3.1
monai==1.3.2

# Model layer (for local unit tests against CPU-only tiny tensors)
torch==2.3.1
torchvision==0.18.1

# Serving layer — FastAPI (FR-5.x)
fastapi==0.111.0
uvicorn[standard]==0.30.1
onnxruntime==1.18.1
pydantic==2.7.4

# Storage / metadata (Section 6.3)
boto3==1.34.131          # S3-compatible / MinIO client
psycopg2-binary==2.9.9   # PostgreSQL

# Experiment tracking / model registry
mlflow==2.14.1

# Monitoring
prometheus-client==0.20.0

# Config
hydra-core==1.3.2
omegaconf==2.3.0

# DVC (data/model versioning — Section 7.3)
dvc[s3]==3.51.2

# Testing & lint (Section 10.3, 11)
pytest==8.2.2
pytest-cov==5.0.0
ruff==0.5.0
mypy==1.10.1
